import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.certifyyaself.campmate',
  appName: 'campermate',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
